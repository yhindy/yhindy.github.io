{
  "dev": {
    "command": "hugo server",
    "description": "Start Hugo development server with live reload at http://localhost:1313"
  },
  "build": {
    "command": "hugo --minify",
    "description": "Build the Hugo site with minification to the public/ directory"
  },
  "test": {
    "command": "hugo --minify && echo 'Build successful - no tests configured'",
    "description": "Build the site to verify it compiles correctly"
  }
}
